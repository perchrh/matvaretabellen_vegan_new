[project]
name = "matvaretabellen-moo"
version = "0.1.0"
description = "Multi-objective optimization for vegan foods from Matvaretabellen"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pymoo",
    "numpy"
]
